# Path to the SQLite database file (shared with brindex-api). Copy this file to .env
# and adjust as needed — .env is gitignored.
BRINDEX_DB_PATH=brindex.sqlite

# Which source to ingest when running run_ingest.sh: treasury-direct, ptax, cdi, or all.
BRINDEX_SOURCE=all
